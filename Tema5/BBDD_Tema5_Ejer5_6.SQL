--A)

SELECT DNI FROM PROFESOR P JOIN DEPARTAMENTO D USING(CODDEP) WHERE D.NOMBRE = 'Informatica y Comunicaciones';

--B)

SELECT NOMBRE, PRAPELLIDO FROM ALUMNO A JOIN ALUMBILL B USING (DNI) WHERE UPPER(LUGAR) IN ('MADRID','BARCELONA');

--C)

SELECT AL.NOMBRE, AL.APELLIDO, AL.SGAPELLIDO FROM ALUMNO AL JOIN MATRICULA M JOIN ASIGNATURA AG USING(DNI) WHERE UPPER(AG.NOMBRE) IN('BASES DE DATOS','ACCESO A DATOS');

--D)

SELECT ALUMNO.NOMBRE, PRAPELLIDO, SGAPELLIDO mFROM ALUMNO AL JOIN MATRICULA M JOIN ASIGNATURA AG JOIN CICLO C USING(CODCF,DNI) WHERE SIGLAS='DAM' AND NOTA>=5;

--E)

SELECT AG.NOMBRE FROM ALUMNO AL JOIN MATRICULA M JOIN ASIGNATURA AG JOIN CICLO C JOIN PROFESOR P JOIN IMPARTE I JOIN DEPARTAMENTO D USING(CODCF,DNI,CODASIG,CODDEP)
WHERE NH>100 AND SIGLAS = 'DAM' AND CICLO.NOMBRE= 'Informatica y Comunicaciones' AND M.CURSO=2023;